{
  "name": "your-project",
  "version": "1.0.0",
  "scripts": {
    "vendor:pyodide": "node scripts/vendor-pyodide.mjs",
    "test:silent-teacher": "node scripts/verify-silent-teacher.mjs",
    "test:db-uebungen": "node scripts/verify-db-uebungen.mjs",
    "postinstall": "node scripts/vendor-pyodide.mjs || true",
    "docs:dev": "npm run vendor:pyodide && vitepress dev docs",
    "docs:build": "npm run vendor:pyodide && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "dev": "npm run vendor:pyodide && vitepress dev docs",
    "build": "npm run vendor:pyodide && vitepress build docs",
    "build-and-dev": "npm run build && vitepress dev docs",
    "deploy": "npm run build-and-push",
    "deploy-github": "npm run build-and-push",
    "build-and-deploy": "npm run build-and-push",
    "serve": "vitepress serve docs/.vitepress/dist",
    "build-and-push": "npm run build && git add . && (git diff --cached --quiet || git commit -m \"Auto-deploy: Built and pushed changes\") && git push origin main"
  },
  "devDependencies": {
    "@codemirror/commands": "^6.11.0",
    "@codemirror/lang-python": "^6.2.1",
    "@codemirror/language": "^6.12.4",
    "@codemirror/state": "^6.7.4",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@codemirror/view": "^6.43.11",
    "postcss": "^8.5.6",
    "pyodide": "^0.27.7",
    "vitepress": "^1.5.0"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "basic-ftp": "^5.0.5",
    "ftp": "^0.3.10",
    "ssh2-sftp-client": "^12.0.1"
  }
}
